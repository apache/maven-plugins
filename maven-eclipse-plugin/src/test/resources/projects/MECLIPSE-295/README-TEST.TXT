This test is run manually.

Its purpose is to check http://jira.codehaus.org/browse/MECLIPSE-295.
The problem was that the sandboxed "test" version was being resolved as a newer version instead of the released version.

Obviously this can't be automatically tested.